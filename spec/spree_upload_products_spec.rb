require 'spec_helper'

describe SpreeUploadProducts do
  it 'has a version number' do
    expect(SpreeUploadProducts::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
